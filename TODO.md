# TODO

[x] convert [ApplicationAccessPolices](https://learn.microsoft.com/en-us/exchange/permissions-exo/application-access-policies) entries into RBAC for application (Convert-ApplicationAccessPolicyToRBAC)

[x] by design get-RBAC4AppEntry should show only RoleAssigneeType -eq 'ServicePrincipal' and (RecipientScope -eq Group) or (RecipientScope -eq CustomRecipientScope))

[x] add to results of  New-RBAC4AppEntry  and New-RBAC4AppUnifiedGroup name of used owner (both requested, and added similarly to member)

[x] rename UnifiedGroupName (and sibling UnifiedGroupExists/UnifiedGroupExisted/UnifiedGroupCreated) to ScopeGroupName/ScopeGroupExists/ScopeGroupExisted/ScopeGroupCreated across New-/Set-/Test-/Remove-RBAC4AppEntry and Invoke-RBAC4AppConfig

[x] warn (and record in Warnings) when -ManagedBy or -BootstrapMember is set to a non-default value for -AccessGroupType MailEnabledSecurityGroup, matching the existing -Members-ignored warning

[x] add MembersFinal (full current group membership, not just what was added) to the results of New-RBAC4AppEntry, Set-RBAC4AppEntry, and Invoke-RBAC4AppConfig

[x] verify that every creation path checks whether the target object (Exchange Online service principal, scope group) already exists and skips creation when it does - New-RBAC4AppUnifiedGroup/New-RBAC4AppDistributionGroup already check before creating, but Register-EXOServicePrincipal calls New-ServicePrincipal unconditionally and relies on its callers (New-/Set-RBAC4AppEntry) to check first

[x] mitigate "Authentication needed. Please call Connect-MgGraph." terminating error from Get-RegisteredAppWithPermission when run in an EXO-only session - it unconditionally calls Get-MgServicePrincipal -ErrorAction Stop with no try/catch to reverse-resolve each EXO assignee, unlike Get-RBAC4AppEntry (Graph is optional there, wrapped in try/catch); should degrade gracefully to EXO-only details + warning instead of throwing, e.g. check Get-MgContext up front or catch the Graph error per assignee

[x] verify if the specific object (ManagementRoleAssignment for the given AppId/ServicePrincipal, or the proposed deterministic assignment name) already exists before creating it - New-RBAC4AppEntry's role-assignment loop calls New-ManagementRoleAssignment -ErrorAction Stop unconditionally with no existence check, unlike Set-RBAC4AppEntry (which checks via Get-ManagementRoleAssignment first and only creates/re-scopes what's missing); if a matching assignment already exists, consider skipping its creation and instead just adding the requested member(s) to the scope group

[x] extend Get-RegisteredAppWithPermission to also report the scope object name and the scope object content (e.g. list of mailboxes/members in the scoping group) - it currently only surfaces DisplayName/AppId/ServicePrincipalId/ExoServicePrincipal/Roles/RoleAssignmentNames/AssignmentCount, none of which expose the recipient scope. The scope group name is already on each raw assignment (RecipientWriteScope/CustomRecipientWriteScope, same fields Set-/Test-/Remove-RBAC4AppEntry already read); the membership would need a per-type lookup (Get-UnifiedGroupLinks/Get-DistributionGroupMember/Get-Recipient) similar to Test-RBAC4AppEntry's dispatch, since the group type isn't known from the assignment alone

[x] split the RBAC4App YAML config schema so scope-group settings are their own top-level RbacScope: section, not mixed into Rbac: - today Rbac: holds both the role list (Roles) and everything about the scope group (AccessGroupType, GroupPrefix, AccessGroupName, Members, ManagedBy, BootstrapMember); move the latter into a new RbacScope: section, keeping Rbac: to just Roles. Touches the private ConvertTo-RBAC4AppYaml/ConvertFrom-RBAC4AppYaml helpers and both New-RBAC4AppConfig (writer) and Invoke-RBAC4AppConfig (reader); this is a breaking change to the YAML schema, so bump SchemaVersion (currently "1.0") and update the round-trip tests/fixtures accordingly

[x] fix wrong scope-group field read for Group-scoped role assignments in Get-RegisteredAppWithPermission (CONFIRMED against a real tenant): for RecipientWriteScope -eq 'Group' (i.e. every assignment this module creates via -RecipientGroupScope), CustomRecipientWriteScope comes back EMPTY; the real scope group name has to be recovered from CustomResourceScope instead, the name of an auto-created ManagementScope object that follows the pattern "<GroupName>_<GUID>" (confirmed against a real tenant sample: "Um365RAo1-Test03_20d5848c-4d61-4b82-a44f-205adc37321f" for group "Um365RAo1-Test03") - CustomRecipientWriteScope is only populated for the (unused-by-this-module) CustomRecipientScope write-scope type. Fixed via a new shared private helper (Resolve-RBAC4AppScopeGroupName) that strips the GUID suffix off CustomResourceScope to recover the group name directly - no extra EXO round trip needed (an earlier version of this fix went through Get-ManagementScope + parsing a guessed recipient-filter format; scrapped as unnecessary and unverified once the direct pattern was spotted in the real sample) - wired into Get-RegisteredAppWithPermission only (read-only, lowest risk). If CustomResourceScope doesn't match the "<GroupName>_<GUID>" pattern (e.g. a scope not created by this module), the raw value is returned as-is and normal downstream resolution just won't find it, same as any other unresolvable scope name.

[x] propagate the Resolve-RBAC4AppScopeGroupName fix (above) to every other place that reads CustomRecipientWriteScope for a Group-scoped assignment, since they all have the identical wrong-field bug: Get-RBAC4AppEntry's Scope output column, Set-RBAC4AppEntry's scopedToTarget re-scope decision, Remove-RBAC4AppEntry's foreign-assignment/safe-to-remove detection, and the New-RBAC4AppEntry/Invoke-RBAC4AppConfig idempotency checks added earlier. Treat Set-/Remove- especially carefully (ConfirmImpact=High, safety-critical logic) - re-confirm the "<GroupName>_<GUID>" naming pattern holds broadly (not just the one real sample seen so far) before relying on it there, since Set-'s re-scope decision and Remove-'s safe-to-delete check are destructive/security-sensitive if wrong

[x] harden Test-RBAC4AppEntry's role-assignment validation: it currently treats an assignment as found when Get-ManagementRoleAssignment -Identity <deterministicName> returns the expected Role, but it does not verify the assignment belongs to the resolved app/service principal or is scoped to the expected scope group. This can make IsValid true for a stale/manual assignment with the same deterministic name and role but the wrong assignee or wrong scope. Check RoleAssigneeName/assignee identity and resolve scope with Resolve-RBAC4AppScopeGroupName before adding to RoleAssignmentsFound.

[x] update tests for Group-scoped assignments to use the real EXO shape documented by Resolve-RBAC4AppScopeGroupName: RecipientWriteScope = 'Group', CustomRecipientWriteScope = $null/empty, and CustomResourceScope = '<GroupName>_<GUID>'. Several tests still mock Group assignments with CustomRecipientWriteScope populated, which lets the wrong-field bugs pass: New-RBAC4AppEntry/Invoke-RBAC4AppConfig idempotency checks, Set-RBAC4AppEntry re-scope checks, Remove-RBAC4AppEntry safety checks, Get-RBAC4AppEntry Scope output, and the new Test-RBAC4AppEntry assignee/scope validation.

[x] merge Get-RegisteredAppWithPermission into Get-RBAC4AppEntry (`-ByApplication` switch for the app-centric view; the deprecated `Get-RegisteredAppWithPermission` is now a thin wrapper around `Get-RBAC4AppEntry -ByApplication -ScopeType All`). Both views now resolve every row's AppId/ServicePrincipalId via a single Get-ServicePrincipal directory read, and its scope group name/type/membership via the new private Resolve-RBAC4AppScope helper (wraps Resolve-RBAC4AppScopeGroupName, adds a Get-UnifiedGroup/Get-DistributionGroup group-type probe using RecipientTypeDetails to tell DistributionList from MailEnabledSecurityGroup, cached per distinct scope name). Added -ScopeType (default Group/CustomRecipientScope, matching the prior hard-coded filter; pass All for every recipient scope, e.g. Organization-wide assignments this module never creates) replacing the previous fixed filter. Remove Get-RegisteredAppWithPermission entirely in a future release once callers have migrated.

[ ] verify against a real tenant that Get-DistributionGroup's RecipientTypeDetails reliably distinguishes 'MailUniversalDistributionGroup' (DistributionList) from 'MailUniversalSecurityGroup' (MailEnabledSecurityGroup) for every scope group this module creates - Resolve-RBAC4AppScope relies on this to populate Get-RBAC4AppEntry's ScopeGroupType column, and it is currently untested against a live tenant (unlike the CustomResourceScope "<GroupName>_<GUID>" pattern it builds on, which was confirmed against one).
